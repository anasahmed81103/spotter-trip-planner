# Base URL of the Django backend's API (must include the /api suffix).
# Local:
VITE_API_BASE_URL=http://localhost:8000/api
# Production example (use your real Render URL):
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
