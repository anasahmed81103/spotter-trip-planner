/**
 * Application root. Renders the single TripPlannerPage - this is a
 * one-page tool, so no router is needed.
 */

import { TripPlannerPage } from "./pages/TripPlannerPage";

function App() {
  return <TripPlannerPage />;
}

export default App;
